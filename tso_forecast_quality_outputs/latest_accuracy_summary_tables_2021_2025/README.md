# Latest accuracy summary tables, 2021-2025

This folder contains regenerated all-case accuracy summary tables for load, solar, onshore wind, and offshore wind.

All tables use the requested column order:

`Country`, `Case`, `Selected model`, `testing period`, `context length`, `MAPE (%)`, `MAE (MW)`, `RMSE (MW)`, `R2`.

The source tables are the latest all-case summaries from `../Load_forecast_new`, `../Solar_forecast_tabpfn_new`, and `../Wind_forecast_new`. See `manifest.csv` for source paths and modification times.
